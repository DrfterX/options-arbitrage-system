# 数据获取
akshare>=1.14.0

# 数值计算
numpy>=1.26.0
scipy>=1.12.0
pandas>=2.2.0

# Web 看板 + 生产服务器
flask>=3.0.0
gunicorn>=23.0.0
flask-cors>=5.0.0
flask-limiter>=4.0.0

# 交易日历
# 主数据源：AKShare Sina 日历（交易所公告汇总，已由 akshare 依赖覆盖）
# 备选降级：chinese_calendar（国务院公告 + 调休处理，可选安装）
# chinese_calendar>=1.11.0

# 测试
pytest>=8.0.0

# 支付（Signals Premium 付费订阅）
stripe>=11.0.0

# 用户认证密码哈希
bcrypt>=4.0.0

# 时区（Windows 需额外安装）
tzdata; platform_system == "Windows"
